# student data — never commit real data (names, phone numbers, uploaded homework files)
data/store.json
data/uploads/

# local dev tools & runtime state (machine-specific, regenerable)
.tools/
scripts/cloudflared.exe
scripts/*.log
scripts/server.pid
scripts/server.mode
